
export interface GenerationVariation {
  patternSelection: boolean;
  sectionSequencing: boolean;
  whitespace: boolean;
  typographyHierarchy: boolean;
  mediaComposition: boolean;
  asymmetry: boolean;
  density: boolean;
  visualMode: boolean;
}
// Variation must NOT come from random gradients, random blobs, arbitrary border radius changes, random shadows, excessive animation, or decorative noise.
