# 19 3D READY PATTERNS

All patterns must accept arbitrary `ReactNode` in media slots. This decoupled architecture allows a future phase to seamlessly inject `<Canvas>` or WebGL scenes into slots like `media` on the `Split` pattern without breaking or modifying the pattern itself.