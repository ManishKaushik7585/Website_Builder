# 14 AI GENERATION RULES

Future AI agents must follow this strict sequence when composing a page:
Research → Objective → IA → Template → Sections → Patterns → Content → Media → Motion → QA

AI MUST NOT generate arbitrary JSX strings. AI must populate a `PageConfig` object and pass it to `PageRenderer`.
