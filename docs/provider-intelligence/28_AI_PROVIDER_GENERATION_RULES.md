# AI PROVIDER GENERATION RULES

## Principle
The runtime treats the AI provider as an untrusted operator that must be bound by strict structural constraints.

Interpret the brief first. Assemble only required context. Generate structured output. Validate output. Never trust raw model output.