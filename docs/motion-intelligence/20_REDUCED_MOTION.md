# Reduced Motion
Mandatory. 
- CSS Level: Use `@media (prefers-reduced-motion: reduce)` to set durations to 0ms.
- JS Level: Use `useReducedMotion()` hook to bypass stagger delays or heavy JS choreography.
Never hide content permanently because animation is disabled.
