# Exit
Exit animations are complex in React without libraries like Framer Motion. Rely on CSS transitions for hiding elements (e.g. Accordion collapse) rather than full unmount animations where possible.
