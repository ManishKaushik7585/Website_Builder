# ANTI VISION SLOP

## Principle
AI Vision observes the rendered result. It does not directly modify the rendered result.
Explicitly reject visual criticism such as "Add a gradient", "Make it pop", "Use a 3D blob" unless explicitly justified by the intent. Fix hierarchy, composition, spacing, content, and structure before adding decoration.