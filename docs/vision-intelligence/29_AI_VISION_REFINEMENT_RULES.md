# AI VISION REFINEMENT RULES

## Principle
AI Vision observes the rendered result. It does not directly modify the rendered result.
Rules: Diagnose before refining. Fix root causes. Prefer subtraction over decoration. Never manipulate the DOM directly. Require evidence for major findings.