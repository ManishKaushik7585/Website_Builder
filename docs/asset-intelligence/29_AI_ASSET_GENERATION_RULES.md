# AI ASSET GENERATION RULES
Before generating an image, the AI must define: Subject, Purpose, Composition, Aspect Ratio, Focal Point, Visual Mode, Lighting, Color Direction, Background, Crop, Breakpoint Behavior. The AI must never generate an image before establishing these parameters.
