# BROWSER PERFORMANCE

## Principle
The browser is the source of truth for rendered behavior. Configuration alone cannot guarantee visual correctness. Never assume a page is visually correct because its configuration is valid.
